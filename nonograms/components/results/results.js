import { createButton } from "../../components/button/button.js";

export function createResultsButton() {
  const button = createButton(["button", "button_results"], "Last results");

  button.addEventListener;

  return button;
}
